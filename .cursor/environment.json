{
  "name": "trial-plus-all-owner-repos-scope",
  "install": "python3 -m pip install -q pytest",
  // Write intent: repositoryDependencies asks Cursor to include every visible
  // d6g8k5htny-coder repo in the Cloud Agent GitHub token scope. Schema items
  // are repo URL strings (https://cursor.com/schemas/environment.schema.json).
  // After this file merges on trial, RELAUNCH a Cloud Agent from trial so the
  // new token picks up full scope — existing runs keep the old trial-only token.
  // Batch 219/223: KEEP ALL owner repos (install currently trial-only; public
  // pull on all). Multi-agent grant: docs/MULTI_AGENT_ACCESS.md +
  // scripts/owner_grant_ai_agent_access.sh (Cursor + Codex + Claude + Grok PAT).
  // google-drive, governance-, main, Math-, meta-framework, query-, trial.
  "repositoryDependencies": [
    "github.com/d6g8k5htny-coder/google-drive",
    "github.com/d6g8k5htny-coder/governance-",
    "github.com/d6g8k5htny-coder/main",
    "github.com/d6g8k5htny-coder/Math-",
    "github.com/d6g8k5htny-coder/meta-framework",
    "github.com/d6g8k5htny-coder/query-",
    "github.com/d6g8k5htny-coder/trial"
  ]
}
