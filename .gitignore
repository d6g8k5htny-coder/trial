# Ignore local virtualenvs and caches; keep the sandbox tiny.
.venv/
__pycache__/
.pytest_cache/
*.pyc
.DS_Store
# Batch 256: runtime flock for concurrent Path B restores
portable/.aligned_drift_restore.lock
